[stages.intake]
model = "haiku"
retry_model = "sonnet"
auth = "subscription"
max_turns = 4
max_attempts = 2
max_output_tokens = 1500
max_input_chars = 24000
max_seconds = 360

[stages.plan]
model = "haiku"
retry_model = "sonnet"
auth = "subscription"
max_turns = 4
max_attempts = 2
max_output_tokens = 2500
max_input_chars = 30000
max_seconds = 360

[stages.build]
model = "haiku"
max_turns = 50
max_seconds = 900
max_total_tokens = 600000
max_cost_usd = 1.50
auth = "subscription"

[stages.evidence]
model = "haiku"
auth = "subscription"
tools = "WebSearch"
max_turns = 12
max_attempts = 2
max_output_tokens = 2500
max_seconds = 300

[stages.panel]
model = "haiku"
auth = "subscription"
max_turns = 4
max_attempts = 2
max_output_tokens = 1200
max_input_chars = 30000
max_seconds = 150

[stages.design]
model = "haiku"
auth = "subscription"
max_turns = 4
max_attempts = 2
max_output_tokens = 2500
max_input_chars = 30000
max_seconds = 360

[stages.repair]
model = "haiku"
auth = "subscription"
max_turns = 30
max_seconds = 900
max_total_tokens = 400000
max_cost_usd = 0.75

[stages.build_split]
model = "haiku"
auth = "subscription"
max_turns = 40
max_seconds = 600
max_total_tokens = 450000
max_cost_usd = 0.90

[stages.verify]
max_seconds = 600

[stages.verify.per_command_seconds]
tsc = 120
eslint = 120
vitest = 180
next_build = 300

[evidence]
min_claims = 3
min_domains = 2

[panel]
min_mean_desirability = 2.5
kill_feasibility_at_or_below = 1

[run]
max_cost_usd = 2.00
max_seconds = 1800

[pricing."claude-haiku-4-5-20251001"]
input = 1.00
output = 5.00
cache_write = 1.25
cache_read = 0.10
