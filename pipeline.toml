[stages.intake]
model = "haiku"
retry_model = "sonnet"
auth = "subscription"
max_turns = 4
max_attempts = 3
max_output_tokens = 2000
max_input_chars = 24000
# covers all attempts; rev-4-sized ideas with a sonnet retry blew 360s (2026-09-03)
max_seconds = 600

[stages.plan]
model = "haiku"
retry_model = "sonnet"
auth = "subscription"
max_turns = 4
max_attempts = 2
max_output_tokens = 2500
max_input_chars = 30000
max_seconds = 360

[stages.build]
model = "sonnet"
max_turns = 50
max_seconds = 900
max_total_tokens = 600000
max_cost_usd = 3.00
auth = "subscription"

[stages.evidence]
model = "haiku"
auth = "subscription"
tools = "WebSearch"
max_turns = 12
max_attempts = 3
max_output_tokens = 2500
max_seconds = 300

[stages.audience]
model = "haiku"
auth = "subscription"
tools = "WebSearch"
max_turns = 16
max_attempts = 3
max_output_tokens = 3000
max_seconds = 420

[stages.domain]
model = "haiku"
auth = "subscription"
tools = "WebSearch"
max_turns = 16
max_attempts = 3
max_output_tokens = 3000
max_seconds = 420

[stages.panel]
model = "haiku"
auth = "subscription"
max_turns = 4
max_attempts = 2
max_output_tokens = 1200
max_input_chars = 30000
max_seconds = 150

[stages.design]
model = "sonnet"
auth = "subscription"
max_turns = 4
max_attempts = 2
max_output_tokens = 2500
max_input_chars = 30000
max_seconds = 360

[stages.repair]
model = "sonnet"
auth = "subscription"
max_turns = 30
max_seconds = 900
max_total_tokens = 400000
max_cost_usd = 2.00

[stages.build_split]
model = "sonnet"
auth = "subscription"
max_turns = 40
max_seconds = 900
max_total_tokens = 450000
max_cost_usd = 2.50

[stages.interview]
model = "haiku"
retry_model = "sonnet"
auth = "subscription"
max_turns = 4
max_attempts = 2
max_output_tokens = 2000
max_input_chars = 30000
max_seconds = 300

[stages.architect]
model = "haiku"
retry_model = "sonnet"
auth = "subscription"
max_turns = 4
max_attempts = 2
max_output_tokens = 2500
max_input_chars = 30000
max_seconds = 360

[stages.ux]
model = "haiku"
retry_model = "sonnet"
auth = "subscription"
max_turns = 4
max_attempts = 2
max_output_tokens = 2500
max_input_chars = 30000
max_seconds = 360

[stages.review]
max_seconds = 60

[stages.security]
max_seconds = 180

[stages.ship]
max_seconds = 600

[stages.analytics]
max_seconds = 60

[stages.verify]
max_seconds = 600

[stages.verify.per_command_seconds]
tsc = 120
eslint = 120
vitest = 180
next_build = 300

[evidence]
min_claims = 3
min_domains = 2

[audience]
min_patterns = 4
min_domains = 2

[domain]
min_findings = 3
min_domains = 2

[panel]
min_mean_desirability = 2.5
kill_feasibility_at_or_below = 1
confirm_margin = 0.34

[run]
# crew scale: 9 LLM stations + a repair pass run ~$2-3 notional (billed 0 on subscription);
# the cap is a runaway stop, not a diet. Raised 2026-09-03 after a green-but-for-lint run
# died at $2.05 right after its repair.
max_cost_usd = 10.00
max_seconds = 3600

[pricing."claude-haiku-4-5-20251001"]
input = 1.00
output = 5.00
cache_write = 1.25
cache_read = 0.10
